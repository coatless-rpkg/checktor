% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-examples.R
\name{lab_example_writes}
\alias{lab_example_writes}
\title{Diagnose Writes Outside the Temporary Directory in Examples}
\usage{
lab_example_writes(path = ".", verbose = TRUE)
}
\arguments{
\item{path}{Character. Path to the package directory. Default: \code{"."}.}

\item{verbose}{Logical. Print diagnostic output. Default: \code{TRUE}.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags a write from an example, vignette or demo whose destination is a literal
path, so it lands in the user's filespace rather than in \code{tempdir()}. A
destination the caller supplies is permission and is not flagged.
}
\section{Source}{

The CRAN Cookbook covers this under
\href{https://contributor.r-project.org/cran-cookbook/code_issues.html#writing-files-and-directories-to-the-home-filespace}{Writing Files and Directories to the Home Filespace},
and the rejection reads "Please ensure that your functions do not write by
default or in your examples/vignettes/tests in the user's home filespace". See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
lab_example_writes(pkg, verbose = FALSE)$passed
}
\seealso{
\code{\link[=checktor]{checktor()}}, \code{\link[=lab_file_operations]{lab_file_operations()}} for the same rule in \verb{R/}.
}
