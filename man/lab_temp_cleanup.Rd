% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-code.R
\name{lab_temp_cleanup}
\alias{lab_temp_cleanup}
\title{Diagnose Missing Temp-File Cleanup}
\usage{
lab_temp_cleanup(path, verbose = TRUE, parsed = NULL)
}
\arguments{
\item{path}{Character. Path to the package directory. Default: \code{"."}.}

\item{verbose}{Logical. Print diagnostic output. Default: \code{TRUE}.}

\item{parsed}{Optional pre-parsed source, as returned internally by the
orchestrator. Defaults to parsing \code{path} afresh.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags a temporary file created without a matching cleanup.
}
\section{Source}{

The CRAN Cookbook covers this under
\href{https://contributor.r-project.org/cran-cookbook/code_issues.html#leaving-files-in-the-temporary-directory}{Leaving Files in the Temporary Directory}.
\code{tempdir()} is removed at session end, so an un-\code{unlink()}ed tempfile breaks no
rule, and tidiness rather than policy is why this sits at \code{opinion} tier. See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
lab_temp_cleanup(pkg, verbose = FALSE)$passed
}
\seealso{
\code{\link[=checktor]{checktor()}}, which runs this and every other check.
}
