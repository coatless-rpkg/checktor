% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-code.R
\name{lab_print_cat_usage}
\alias{lab_print_cat_usage}
\title{Diagnose Print/Cat Usage in Functions}
\usage{
lab_print_cat_usage(path, verbose = TRUE, parsed = NULL)
}
\arguments{
\item{path}{Character. Path to package directory.}

\item{verbose}{Logical. Print diagnostic messages.}

\item{parsed}{Internal. Pre-parsed source cache; if \code{NULL}, files are read
from \code{path} on demand.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags \code{print()} / \code{cat()} calls not guarded by an enclosing \verb{if()},
\verb{for()}, or \verb{while()}. The check uses the ancestor axis, so guard
detection is robust regardless of formatting. Calls inside S3 \verb{print.*}
and \verb{format.*} methods are exempt, since \code{cat()} is the required idiom
there (base R's own \code{print.default()} / \code{print.lm()} use it).
}
\section{Source}{

The CRAN Cookbook covers this under
\href{https://contributor.r-project.org/cran-cookbook/code_issues.html#using-printcat}{Using print()/cat()}.
Diagnostic output belongs in \code{message()} or \code{warning()}, which a user can
suppress, rather than in \code{cat()} or \code{print()}, which they cannot. Neither the
Repository Policy nor Writing R Extensions states this, but reviewers ask for it
consistently. See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg <- example_diagnose_scenario("code_examples/print_cat_bad.R",
                                 show_content = FALSE)
lab_print_cat_usage(pkg, verbose = FALSE)
}
