% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-general.R
\name{lab_url_liveness}
\alias{lab_url_liveness}
\title{Diagnose Broken and Redirecting URLs}
\usage{
lab_url_liveness(path, verbose = TRUE)
}
\arguments{
\item{path}{Character. Path to package directory}

\item{verbose}{Logical. Print diagnostic messages}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Fetches every URL in the package, across DESCRIPTION, \code{.Rd} files and
vignettes, and reports the ones that fail: 404s, other error statuses, and
redirects that ought to point at their final target. This is what
\verb{R CMD check --as-cran} does, through the same base R machinery
(\code{\link[tools:check_package_urls]{tools::check_package_urls()}}), so you can see those findings without a full
\code{--as-cran} pass and without depending on the \code{urlchecker} package.
}
\details{
It runs by default when you are working at the console, since that is when a
broken link is worth knowing about and a pause for the network is fine. It stays
off in scripts, in continuous integration and under \verb{R CMD check}, where a slow
or unreachable network would make results depend on the machine rather than the
package. Set the option either way to decide for yourself:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options(checktor.url_check = TRUE)   # always check
options(checktor.url_check = FALSE)  # never check
}\if{html}{\out{</div>}}

Without a network the fetch reports nothing and the check passes quietly, just
as CRAN's own URL check does. For the offline half, which flags \verb{http://} links
and URL shorteners without leaving the room, see \code{\link[=lab_urls]{lab_urls()}}.
}
\section{Source}{

The \href{https://cran.r-project.org/doc/manuals/r-release/R-exts.html#Checking-packages}{CRAN incoming check}
run by \verb{R CMD check --as-cran} fetches URLs and NOTEs 404s and redirects. See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
# Needs a network, so this is not run automatically:
\dontrun{
lab_url_liveness(".")
}
}
