% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{find_package_root}
\alias{find_package_root}
\title{Find the Root of the Package Containing a Path}
\usage{
find_package_root(path = ".")
}
\arguments{
\item{path}{Character. A directory inside a package, or a file within one.
Default: \code{"."}.}
}
\value{
Character. The package root, as an absolute path, when one is found above
\code{path}. When \code{path} itself holds a \code{DESCRIPTION} it is returned unchanged, so
an existing caller sees exactly what it passed in. When no package root is
found at all, the search falls back to the directory it started from, meaning
\code{path} for a directory and its parent for a file, which leaves the caller
reporting the problem against the place the user pointed at.
}
\description{
Walks up from \code{path} until it finds the directory holding a \code{DESCRIPTION}
file, so checktor can be run from anywhere inside a package tree rather than
only from the directory holding \code{DESCRIPTION}. That is what lets \code{checktor()}
work with your working directory set to \verb{R/}, \verb{tests/testthat/}, or any other
subdirectory.
}
\details{
Every checktor entry point calls this on the \code{path} it is given, so you
rarely need it directly. It is exported for custom checks registered with
\code{\link[=register_check]{register_check()}}, which receive a path that has already been resolved.
}
\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)

# From the package root, the path is handed back untouched
identical(find_package_root(pkg), pkg)

# From a subdirectory, the root is found by walking up
basename(find_package_root(file.path(pkg, "R")))
}
\seealso{
\code{\link[=checktor]{checktor()}}, which resolves its \code{path} this way.
}
