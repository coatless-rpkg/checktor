% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci-report.R
\name{ci_report}
\alias{ci_report}
\title{Report Findings in the Format Your CI Understands}
\usage{
ci_report(
  results = NULL,
  format = c("auto", "github", "gitlab", "checkstyle", "sarif", "azure", "text"),
  file = NULL,
  severity = SEVERITY_LEVELS,
  skipped = TRUE,
  path = "."
)
}
\arguments{
\item{results}{A \code{checktor_results} object. Defaults to running \code{\link[=checktor]{checktor()}} on
\code{path} quietly, so a build can call this on its own.}

\item{format}{Character. One of \code{"auto"}, \code{"github"}, \code{"gitlab"},
\code{"checkstyle"}, \code{"sarif"}, \code{"azure"} or \code{"text"}. \code{"auto"} reads the
environment variables each forge sets.}

\item{file}{Character. Where to write. Defaults to standard output for the
comment-style formats and to a conventional file name for the report styles.}

\item{severity}{Character. Which tiers to report. Defaults to every tier, since
an annotation is information rather than a verdict.}

\item{skipped}{Logical. Report the checks that did not run. Defaults to \code{TRUE},
so a green pipeline never implies a check that never happened. Named once in
aggregate rather than one annotation per check.}

\item{path}{Character. The package to examine when \code{results} is not supplied.}
}
\value{
Invisibly, the character vector written.
}
\description{
Renders a \code{\link[=checktor]{checktor()}} result as machine-readable findings, so a build puts each
one next to the line that caused it instead of leaving it in a log for someone
to read. The format defaults to whichever forge the build is running on.
}
\details{
Each forge reads a different shape, and \code{format} picks it:
\itemize{
\item \code{"github"} writes workflow commands to standard output, which GitHub Actions
turns into annotations on the pull request diff. Gitea and Forgejo Actions
read the same commands.
\item \code{"gitlab"} writes a Code Quality report, which GitLab shows on the merge
request diff. Name the file in \verb{artifacts:reports:codequality:}.
\item \code{"checkstyle"} writes Checkstyle XML, which Jenkins, reviewdog and most
review bots read. This is the one to reach for on a forge with no format of
its own.
\item \code{"sarif"} writes SARIF 2.1.0, which GitHub code scanning and Azure ingest.
\item \code{"azure"} writes Azure Pipelines logging commands.
\item \code{"text"} writes one plain line per finding, for a build with no forge at all.
}

Findings carry a file name rather than a path, so the path is recovered by
looking for the file under \verb{R/}, \verb{man/}, \verb{vignettes/} and the other places a
package keeps code. A finding with no location at all, such as a \code{DESCRIPTION}
field problem, is reported against \code{DESCRIPTION} so it still appears.
}
\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
results <- checktor(pkg, verbose = FALSE, progress = FALSE)

# What a GitHub Actions job would emit
writeLines(head(ci_report(results, format = "github", file = NULL), 3))
}
\seealso{
\code{\link[=checkup]{checkup()}} for the pass or fail gate, \code{\link[=health_report]{health_report()}} for a report
a person reads.
}
