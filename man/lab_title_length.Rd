% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-description.R
\name{lab_title_length}
\alias{lab_title_length}
\title{Diagnose Title Length}
\usage{
lab_title_length(path = ".", verbose = TRUE, desc = NULL)
}
\arguments{
\item{path}{Character. Path to the package directory. Default: \code{"."}.}

\item{verbose}{Logical. Print diagnostic output. Default: \code{TRUE}.}

\item{desc}{Optional pre-parsed \code{DESCRIPTION}, as returned by \code{\link[base:read.dcf]{base::read.dcf()}}.
Defaults to reading it from \code{path}.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags a \code{Title} longer than 65 characters.
}
\section{Source}{

\emph{Writing R Extensions} §1.1.1 notes that some package listings may truncate the
title to 65 characters. That is a display width rather than a limit, so a title
of exactly 65 characters still shows in full and only a longer one loses its
tail. Nothing rejects a long title, which is why this sits at \code{opinion} tier.
See \code{vignette("check-sources", package = "checktor")} for how every check maps
to its source.
}

\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
lab_title_length(pkg, verbose = FALSE)$passed
}
\seealso{
\code{\link[=checktor]{checktor()}}, which runs this and every other check.
}
