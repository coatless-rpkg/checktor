% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-policy.R
\name{lab_system_calls}
\alias{lab_system_calls}
\title{Diagnose System Calls}
\usage{
lab_system_calls(path, verbose = TRUE, parsed = NULL)
}
\arguments{
\item{path}{Character. Path to the package directory. Default: \code{"."}.}

\item{verbose}{Logical. Print diagnostic output. Default: \code{TRUE}.}

\item{parsed}{Optional pre-parsed source, as returned internally by the
orchestrator. Defaults to parsing \code{path} afresh.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags \code{system()} / \code{system2()} / \code{shell()}, which need review for portability and for shell-injection risk.
}
\section{Source}{

No flat rule. A raw \code{system()} or \code{system2()} call needs review for
portability rather than being an automatic violation, which is why this sits
at \code{robustness} tier. See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
lab_system_calls(pkg, verbose = FALSE)$passed
}
\seealso{
\code{\link[=checktor]{checktor()}}, which runs this and every other check.
}
