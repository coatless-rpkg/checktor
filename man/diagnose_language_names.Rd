% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-description.R
\name{diagnose_language_names}
\alias{diagnose_language_names}
\title{Diagnose Programming-Language Names in DESCRIPTION}
\usage{
diagnose_language_names(path = ".", verbose = TRUE, desc = NULL)
}
\arguments{
\item{path}{Character. Path to the package directory. Default: \code{"."}.}

\item{verbose}{Logical. Print diagnostic output. Default: \code{TRUE}.}

\item{desc}{Optional pre-parsed \code{DESCRIPTION}, as returned by \code{\link[base:read.dcf]{base::read.dcf()}}.
Defaults to reading it from \code{path}.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags a bare programming-language, markup, or statistical-computing name --
\code{Python}, \code{Java}, \verb{C++}, \code{SQL}, \code{HTML}, \code{MATLAB}, \code{SAS} and more -- in \code{Title}
or \code{Description} that CRAN asks to see single-quoted. This is the language
counterpart to \code{\link[=diagnose_software_names_formatting]{diagnose_software_names_formatting()}}: both are policy-tier
quoting checks, kept separate because a language name and a package name are
different kinds of thing. \code{R} itself is never flagged -- it is the host language
and appears too often to quote sensibly, and single-letter or common-word names
(\code{C}, \code{Go}, \code{Swift}) are left out for the same reason.
}
\details{
A package can extend the list through \code{Config/checktor/language_names} in its own
DESCRIPTION.
}
\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
diagnose_language_names(pkg, verbose = FALSE)$passed
}
\seealso{
\code{\link[=checktor]{checktor()}}, \code{\link[=diagnose_software_names_formatting]{diagnose_software_names_formatting()}}.
}
