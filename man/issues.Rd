% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{issues}
\alias{issues}
\alias{issues.checktor_check_result}
\alias{issues.checktor_category_result}
\alias{issues.checktor_results}
\title{Extract issues, checks, or a per-category summary from checktor results}
\usage{
issues(x, ...)

\method{issues}{checktor_check_result}(x, ...)

\method{issues}{checktor_category_result}(x, ...)

\method{issues}{checktor_results}(x, ...)
}
\arguments{
\item{x}{A \code{checktor_results}, \code{checktor_category_result}, or
\code{checktor_check_result} object.}

\item{...}{Unused.}
}
\value{
\code{issues()} returns a \code{data.frame} with one row per issue. At the
results level the columns are \code{category}, \code{check}, \code{file}, \code{line},
\code{location}, \code{message}; a single category drops \code{category}; a single check
drops \code{category} and \code{check}. A healthy object yields a 0-row frame.
}
\description{
Plain accessors over the objects returned by \code{\link[=checktor]{checktor()}} and the
\verb{diagnose_*_issues()} functions, so you never navigate nested sublists.
}
\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
results <- checktor(pkg, verbose = FALSE, progress = FALSE)
issues(results)
}
