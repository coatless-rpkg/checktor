% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-description.R
\name{diagnose_spelling}
\alias{diagnose_spelling}
\title{Diagnose Possibly Misspelled Words in DESCRIPTION}
\usage{
diagnose_spelling(path = ".", verbose = TRUE, desc = NULL)
}
\arguments{
\item{path}{Character. Path to the package directory. Default: \code{"."}.}

\item{verbose}{Logical. Print diagnostic output. Default: \code{TRUE}.}

\item{desc}{Present for signature parity with the other DESCRIPTION checks;
spelling reads the \code{DESCRIPTION} file directly and ignores it.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}; the
\code{issues} are the possibly-misspelled words.
}
\description{
Spell-checks the \code{Title} and \code{Description} fields with \code{\link[utils:aspell]{utils::aspell()}},
mirroring the aspell pass in CRAN's incoming check. It reports only words that
are not already accepted somewhere: a package \verb{.aspell/} dictionary,
\code{inst/WORDLIST}, or a \code{Config/checktor/acronyms} or \code{software_names} field.
}
\details{
The check needs a spell-check backend (\code{aspell} or \code{hunspell}) on the system.
Without one it passes quietly, the same way CRAN's incoming check skips
spelling when no backend is present, so a run on one machine may find words a
run on another does not. It is therefore an \code{opinion}-tier check. When it does
fire, \code{\link[=prescribe]{prescribe()}} hands back a ready-to-paste \verb{.aspell/} snippet with the
flagged words filled in.
}
\section{Source}{

The \href{https://cran.r-project.org/doc/manuals/r-release/R-exts.html#Checking-packages}{CRAN incoming check}
runs \code{aspell} over the \code{Title} and \code{Description}; it needs a
spell-check backend and is noisy, so checktor keeps it at
\code{opinion} tier. See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
diagnose_spelling(pkg, verbose = FALSE)$passed
}
\seealso{
\code{\link[=checktor]{checktor()}}, \code{\link[=prescribe]{prescribe()}}.
}
