% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-code.R
\name{diagnose_detect_cores_robustness}
\alias{diagnose_detect_cores_robustness}
\title{Diagnose Unguarded \code{detectCores()}}
\usage{
diagnose_detect_cores_robustness(path, verbose = TRUE, parsed = NULL)
}
\arguments{
\item{path}{Character. Path to package directory.}

\item{verbose}{Logical. Print diagnostic messages.}

\item{parsed}{Internal. Pre-parsed source cache; if \code{NULL}, files are read
from \code{path} on demand.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags a \code{parallel::detectCores()} call whose result is used without an \code{NA}
guard.
}
\details{
\code{?detectCores} says so in as many words: \emph{"An integer, \code{NA} if the answer is
unknown"}, and R's own advice is to avoid it, \emph{"First because it may return
\code{NA}"}. \code{NA} then propagates silently through the arithmetic packages usually
do next, and the failure surfaces far from its cause:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{n <- parallel::detectCores() - 1   # NA - 1 is NA
if (cores > n) cores <- n          # Error: missing value where TRUE/FALSE needed
}\if{html}{\out{</div>}}

This is a robustness defect rather than a policy one, and it is distinct from
the \code{core_usage} check, which asks how many cores you \emph{use}. A package can cap
itself at two cores perfectly and still crash on the machine where
\code{detectCores()} returns \code{NA}.

A call is treated as guarded when its enclosing function tests for \code{NA}
(\code{is.na()}), passes \code{na.rm = TRUE}, or supplies a fallback with \verb{\%||\%}. The
durable fix is \code{parallelly::availableCores()}, which never returns \code{NA} and
also honours the CRAN core limit.
}
\section{Source}{

No formal rule. \code{?detectCores} states it returns "\code{NA} if the answer is
unknown", and the arithmetic that usually follows then crashes, which is why
this sits at \code{robustness} tier. See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
diagnose_detect_cores_robustness(pkg, verbose = FALSE)$passed
}
