% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ast.R
\name{not_under_fn_with_call_xpath}
\alias{not_under_fn_with_call_xpath}
\title{XPath Predicate: Not Guarded by a Sibling Call}
\usage{
not_under_fn_with_call_xpath(funs)
}
\arguments{
\item{funs}{Character vector of guard function names (e.g. \code{"on.exit"}).}
}
\value{
A single character string: an XPath predicate to splice into a query
after a node test.
}
\description{
Returns an XPath predicate that restricts matches to nodes whose innermost
enclosing function body does \emph{not} also contain a call to any of \code{funs}. This
is how a check enforces a guard, for instance that an \code{options()} call is
paired with an \code{on.exit()} in the same function.
}
\details{
The predicate anchors on \code{ancestor::expr[FUNCTION][1]}, the nearest function
definition, and searches its whole subtree. Anchoring on the innermost
function keeps it correct where the guard belongs to an inner function rather
than an outer one, and covers a call sitting in a default argument as well as
one in the body.
}
\examples{
predicate <- not_under_fn_with_call_xpath(c("on.exit", "local_options"))
paste0("//SYMBOL_FUNCTION_CALL[text() = 'options'][", predicate, "]")
}
\seealso{
\code{\link[=xpath_lints]{xpath_lints()}}.
}
