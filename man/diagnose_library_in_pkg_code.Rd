% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-code.R
\name{diagnose_library_in_pkg_code}
\alias{diagnose_library_in_pkg_code}
\title{Diagnose library() in Package Code}
\usage{
diagnose_library_in_pkg_code(path, verbose = TRUE, parsed = NULL)
}
\arguments{
\item{path}{Character. Path to the package directory. Default: \code{"."}.}

\item{verbose}{Logical. Print diagnostic output. Default: \code{TRUE}.}

\item{parsed}{Optional pre-parsed source, as returned internally by the
orchestrator. Defaults to parsing \code{path} afresh.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags \code{library()} / \code{require()} in package code, which alters the user's search path. Code destined for a parallel worker is exempt, since a daemon starts with an empty search path.
}
\section{Source}{

\href{https://cran.r-project.org/doc/manuals/r-release/R-exts.html#Package-Dependencies}{Writing R Extensions},
under "Package Dependencies", asks package code to reach its dependencies
through \code{Imports} and \code{::} rather than attaching them with \code{library()}. See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
diagnose_library_in_pkg_code(pkg, verbose = FALSE)$passed
}
\seealso{
\code{\link[=checktor]{checktor()}}, which runs this and every other check.
}
