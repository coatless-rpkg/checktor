% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-code.R
\name{lab_globalenv_mod}
\alias{lab_globalenv_mod}
\title{Diagnose Writes to the Global Environment}
\usage{
lab_globalenv_mod(path, verbose = TRUE, parsed = NULL)
}
\arguments{
\item{path}{Character. Path to the package directory. Default: \code{"."}.}

\item{verbose}{Logical. Print diagnostic output. Default: \code{TRUE}.}

\item{parsed}{Optional pre-parsed source, as returned internally by the
orchestrator. Defaults to parsing \code{path} afresh.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags a \verb{<<-} whose target binds in neither an enclosing function nor the package, and so reaches \code{.GlobalEnv}. A closure updating its parent, or a package-level cache, is not flagged.
}
\section{Source}{

The \href{https://cran.r-project.org/web/packages/policies.html}{CRAN Repository Policy}
states that "Packages should not modify the global environment (user's
workspace)." See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
lab_globalenv_mod(pkg, verbose = FALSE)$passed
}
\seealso{
\code{\link[=checktor]{checktor()}}, which runs this and every other check.
}
