% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-examples.R
\name{lab_example_interactive}
\alias{lab_example_interactive}
\title{Diagnose Interactive Examples Wrapped in \verb{\\\dontrun{}}}
\usage{
lab_example_interactive(path = ".", verbose = TRUE)
}
\arguments{
\item{path}{Character. Path to the package directory. Default: \code{"."}.}

\item{verbose}{Logical. Print diagnostic output. Default: \code{TRUE}.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags an \verb{\\\\examples\{\}} block that hides an interactive function behind
\verb{\\\dontrun{}}. CRAN asks for \verb{if (interactive())} instead, so a reader can see
the function is not meant for a script rather than only that it does not run.
}
\section{Source}{

The CRAN Cookbook covers this under
\href{https://contributor.r-project.org/cran-cookbook/general_issues.html#structuring-of-examples}{Structuring of Examples},
and the rejection reads "Functions which are supposed to only run interactively
(e.g. shiny) should be wrapped in if(interactive()). Please replace \\dontrun{}
with if(interactive()){} if possible". See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
lab_example_interactive(pkg, verbose = FALSE)$passed
}
\seealso{
\code{\link[=checktor]{checktor()}}, \code{\link[=lab_example_structure]{lab_example_structure()}}.
}
