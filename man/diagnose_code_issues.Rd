% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-code.R
\name{diagnose_code_issues}
\alias{diagnose_code_issues}
\title{Diagnose Code Health Issues}
\usage{
diagnose_code_issues(path = ".", verbose = TRUE)
}
\arguments{
\item{path}{Character. Path to the R package directory. Default: \code{"."}.}

\item{verbose}{Logical. Whether to print detailed diagnostic output.
Default: \code{TRUE}.}
}
\value{
List of named \code{\link[=checktor_check_result]{checktor_check_result()}} objects (e.g., \code{tf_usage},
\code{seed_setting}) plus a \code{passed} named logical vector summarizing pass/fail
for each sub-check.
}
\description{
Runs comprehensive diagnostics on R source code to identify common CRAN
submission issues and coding best-practice violations.
}
\details{
Each source file is parsed once with \code{parse(keep.source = TRUE)}; checks
run XPath queries against the parsed XML representation, so identifiers
that appear only inside string literals or comments do not false-positive.
Multi-line constructs (\verb{set.seed(\\n123\\n)}), formula \code{~} versus path \code{~},
and scope-aware patterns (an \code{options()} call guarded by a sibling
\code{on.exit()} in the same function body) are all handled correctly.
}
\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
code_results <- diagnose_code_issues(pkg, verbose = FALSE)
code_results$tf_usage$passed
}
\seealso{
\code{\link[=checktor]{checktor()}} for complete package diagnostics
}
