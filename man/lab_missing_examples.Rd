% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-documentation.R
\name{lab_missing_examples}
\alias{lab_missing_examples}
\title{Diagnose Exported Functions Missing Examples}
\usage{
lab_missing_examples(path, verbose = TRUE)
}
\arguments{
\item{path}{Character. Path to package directory}

\item{verbose}{Logical. Print diagnostic messages}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{missing},
\code{message}.
}
\description{
CRAN expects exported functions to carry a runnable \verb{\\examples\{\}} section.
Walks \code{.Rd} files via \code{\link[tools:parse_Rd]{tools::parse_Rd()}} and reports exported function
topics that lack one. Data, class, methods, package-level, and re-export
topics are skipped, and only topics whose name appears in NAMESPACE
\code{export()} are considered (so internal helpers and S3 methods aren't
required to have examples). A function that exists only for its side effect
may be reported here even though it is fine, so use your judgement.
}
\section{Source}{

The CRAN Cookbook covers examples under
\href{https://contributor.r-project.org/cran-cookbook/general_issues.html#structuring-of-examples}{Structuring of Examples}.
Exported functions are expected to carry an \verb{\\examples\{\}} block, a convention
rather than a rule, which is why this sits at \code{opinion} tier. See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg_path <- example_diagnose_scenario(
  "documentation_examples/missing_examples_bad.Rd", show_content = FALSE)
writeLines("export(undocumented_fn)", file.path(pkg_path, "NAMESPACE"))
issues(lab_missing_examples(pkg_path, verbose = FALSE))
}
