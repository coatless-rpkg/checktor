% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-general.R
\name{lab_urls}
\alias{lab_urls}
\title{Diagnose URL Issues in Package Files}
\usage{
lab_urls(path, verbose = TRUE)
}
\arguments{
\item{path}{Character. Path to package directory}

\item{verbose}{Logical. Print diagnostic messages}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags \verb{http://} URLs (which should almost always be \verb{https://}) and known URL
shortener domains, across DESCRIPTION, README, vignettes and the \code{.Rd} files.
}
\details{
This is a fast, \strong{offline} pre-flight. \verb{R CMD check --as-cran} does fetch every
URL and report status codes and redirect targets, but only with a network and
only under \code{--as-cran}, which is the slow end of the loop. This catches the two
problems you can find without leaving the room, before you spend ten minutes on
a full check.

Literal spans are skipped, so documenting the string \verb{http://} inside \verb{\verb{}},
\verb{\code{}} or a fenced markdown block is not mistaken for linking to it.
}
\section{Source}{

No formal rule. Preferring \verb{https://} is good advice, but CRAN's NOTE is
about broken URLs rather than the scheme, which is why this sits at \code{opinion}
tier. See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg_path <- example_diagnose_scenario("description_examples/bad_description.txt",
                                      show_content = FALSE)
issues(lab_urls(pkg_path, verbose = FALSE))
}
