% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-code.R
\name{lab_internal_ns}
\alias{lab_internal_ns}
\title{Diagnose \code{:::} in Package Code}
\usage{
lab_internal_ns(path, verbose = TRUE, parsed = NULL)
}
\arguments{
\item{path}{Character. Path to package directory.}

\item{verbose}{Logical. Print diagnostic messages.}

\item{parsed}{Internal. Pre-parsed source cache; if \code{NULL}, files are read
from \code{path} on demand.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags a \code{pkg:::fn()} call in \verb{R/}. The triple colon reaches an object another
package does not export, whose behaviour its author is free to change in routine
maintenance, so a release elsewhere can break your package without warning.
}
\details{
A call into your own package is reported too, since a package almost never needs
\code{:::} for its own objects: everything in the namespace is already visible to the
rest of it.
}
\section{Source}{

CRAN sends this back as "Using foo:::f instead of foo::f allows access to
unexported objects. This is generally not recommended ... Please omit one
colon." \verb{R CMD check} reports it too, under dependencies in R code, so this
check is the same finding without waiting for a full check. See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R",
                                 show_content = FALSE)
lab_internal_ns(pkg, verbose = FALSE)$passed
}
\seealso{
\code{\link[=checktor]{checktor()}}, \code{\link[=lab_example_internal_ns]{lab_example_internal_ns()}} for the same rule in examples.
}
