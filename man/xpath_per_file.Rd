% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ast.R
\name{xpath_per_file}
\alias{xpath_per_file}
\title{Summarise XPath Matches per File}
\usage{
xpath_per_file(parsed, xpath, summarise)
}
\arguments{
\item{parsed}{A parsed-sources list from \code{\link[=read_r_xml]{read_r_xml()}}.}

\item{xpath}{Character. An XPath 1.0 query.}

\item{summarise}{A function of \verb{(file, nodes)} returning a character vector of
issue strings, where \code{nodes} is an \code{xml2} nodeset.}
}
\value{
A character vector of the issue strings \code{summarise} produced.
}
\description{
A per-file variant of \code{\link[=xpath_lints]{xpath_lints()}} for when you need to control the issue
string. Runs \code{xpath} against each parsed file and calls \code{summarise(file, nodes)} on each non-empty match set, collecting the strings it returns.
}
\examples{
pkg <- example_diagnose_scenario("code_examples/seed_setting_bad.R",
                                 show_content = FALSE)
parsed <- read_r_xml(pkg)
xpath_per_file(parsed, "//SYMBOL_FUNCTION_CALL[text() = 'set.seed']",
               function(file, nodes) {
                 paste0(basename(file), ":", xml2::xml_attr(nodes, "line1"))
               })
}
\seealso{
\code{\link[=xpath_lints]{xpath_lints()}}.
}
