% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-code.R
\name{diagnose_hardcoded_credentials}
\alias{diagnose_hardcoded_credentials}
\title{Diagnose Hardcoded Credentials in Package Code}
\usage{
diagnose_hardcoded_credentials(path, verbose = TRUE, parsed = NULL)
}
\arguments{
\item{path}{Character. Path to package directory.}

\item{verbose}{Logical. Print diagnostic messages.}

\item{parsed}{Internal. Pre-parsed source cache; if \code{NULL}, files are read
from \code{path} on demand.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags a secret that looks like an API key, access token, or private key
sitting in a string literal in \verb{R/}. \verb{R CMD check} does not scan for leaked
credentials, yet a token committed to a package is a security problem: once
the package is published to CRAN the secret is public and must be revoked.
}
\details{
Only string literals in the parsed sources are examined, so the same text in
a comment or a variable name never matches, and the check's own pattern
strings never flag themselves.

Each format is anchored on a provider-specific prefix so ordinary code does
not match. The recognised credentials, and the prefix each is keyed on, are:
\itemize{
\item \strong{Version control and registries}: GitHub tokens (\code{ghp_}, \code{gho_}, \code{ghu_},
\code{ghs_}, \code{ghr_}, and fine-grained \code{github_pat_}), GitLab tokens (\verb{glpat-}),
npm tokens (\code{npm_}), and PyPI upload tokens (\code{pypi-AgEIcHlwaS5vcmc}).
\item \strong{Cloud providers}: AWS access keys (\code{AKIA}, \code{ASIA}, \code{AROA}, \code{AIDA},
\code{AGPA}, \code{ABIA}, \code{ACCA}), Google API keys (\code{AIza}), Google OAuth client
secrets (\verb{GOCSPX-}), and DigitalOcean tokens (\code{dop_v1_}).
\item \strong{AI and ML providers}: OpenAI keys (\verb{sk-}, \verb{sk-proj-}), Anthropic keys
(\verb{sk-ant-}), and Hugging Face tokens (\code{hf_}).
\item \strong{Payments}: Stripe secret and restricted keys (\code{sk_live_}, \code{sk_test_},
\code{rk_live_}, \code{rk_test_}) and Square tokens (\verb{sq0atp-}, \verb{sq0csp-}, \code{EAAA}).
\item \strong{Messaging}: Slack tokens (\verb{xoxb-}, \verb{xoxp-}, ...) and incoming webhooks
(\code{hooks.slack.com/services/...}), SendGrid keys (\code{SG.}), and Telegram bot
tokens (\verb{<id>:AA...}).
\item \strong{Platforms}: Shopify tokens (\code{shpat_}, \code{shpss_}, \code{shpca_}, \code{shppa_}),
Databricks tokens (\code{dapi}), and Postman keys (\verb{PMAK-}).
\item \strong{Generic}: JSON Web Tokens (\code{eyJ...}) and PEM private-key headers
(\verb{-----BEGIN ... PRIVATE KEY-----}).
}

The prefixes and lengths follow each provider's published token format and
the community-maintained gitleaks secret-detection ruleset.
}
\section{Source}{

No formal rule. A token or key committed to a package is public the moment it
reaches CRAN and must be revoked, which is why this sits at \code{robustness}
tier. See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
pkg <- example_diagnose_scenario("code_examples/credentials_bad.R",
                                 show_content = FALSE)
diagnose_hardcoded_credentials(pkg, verbose = FALSE)$passed
}
\references{
Provider token formats and the gitleaks ruleset:
\url{https://github.com/gitleaks/gitleaks}
}
