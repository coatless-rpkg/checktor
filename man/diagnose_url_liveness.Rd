% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-general.R
\name{diagnose_url_liveness}
\alias{diagnose_url_liveness}
\title{Diagnose Broken and Redirecting URLs (Opt-In, Network)}
\usage{
diagnose_url_liveness(path, verbose = TRUE)
}
\arguments{
\item{path}{Character. Path to package directory}

\item{verbose}{Logical. Print diagnostic messages}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Fetches every URL in the package -- DESCRIPTION, \code{.Rd} files, and vignettes --
and reports the ones that fail: 404s, other error statuses, and redirects that
ought to point at their final target. This is exactly what
\verb{R CMD check --as-cran} does, through the same base R machinery
(\code{\link[tools:check_package_urls]{tools::check_package_urls()}}); checktor simply surfaces it as a check so you
can run it without a full \code{--as-cran} pass and without depending on the
\code{urlchecker} package.
}
\details{
Because it needs a network and is comparatively slow, it is \strong{opt-in} and does
nothing until you enable it:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options(checktor.url_check = TRUE)
checktor(".")
}\if{html}{\out{</div>}}

With the option unset -- or when the fetch cannot run, such as offline -- it
passes quietly, exactly as CRAN's own URL check does without connectivity. For
the fast, offline half (flagging \verb{http://} and URL shorteners without leaving
the room) see \code{\link[=diagnose_urls]{diagnose_urls()}}.
}
\section{Source}{

The \href{https://cran.r-project.org/doc/manuals/r-release/R-exts.html#Checking-packages}{CRAN incoming check}
run by \verb{R CMD check --as-cran} fetches URLs and NOTEs 404s and redirects; it
is opt-in here because it needs a network. See
\code{vignette("check-sources", package = "checktor")} for how every check maps to its
source.
}

\examples{
# Opt in first, then run against a package directory (needs a network):
\dontrun{
options(checktor.url_check = TRUE)
diagnose_url_liveness(".")
}
}
