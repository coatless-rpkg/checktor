% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ast.R
\name{collect_rd_text}
\alias{collect_rd_text}
\title{Flatten a Parsed \code{.Rd} Node to Text}
\usage{
collect_rd_text(node, skip = character(0))
}
\arguments{
\item{node}{An Rd node, such as one returned by \code{\link[=extract_rd_section]{extract_rd_section()}}.}

\item{skip}{Character vector of \code{Rd_tag} values to omit from the text.}
}
\value{
A single character string.
}
\description{
Recursively concatenates the text of a \code{\link[tools:parse_Rd]{tools::parse_Rd()}} node, optionally
skipping subsections by \code{Rd_tag} (for instance \code{"\\\dontrun"} when collecting
example code that is meant to run).
}
\examples{
rd_file <- tempfile(fileext = ".Rd")
writeLines(c("\\\\name{foo}", "\\\\title{Foo}",
             "\\\\examples{ f() \\\\dontrun{ g() } }"), rd_file)
rd <- tools::parse_Rd(rd_file)
collect_rd_text(extract_rd_section(rd, "\\\\examples"), skip = "\\\\dontrun")
}
\seealso{
\code{\link[=extract_rd_section]{extract_rd_section()}}.
}
