% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-code.R
\name{lab_tf_usage}
\alias{lab_tf_usage}
\title{Diagnose \code{T}/\code{F} Usage in R Code}
\usage{
lab_tf_usage(path, verbose = TRUE, parsed = NULL)
}
\arguments{
\item{path}{Character. Path to package directory.}

\item{verbose}{Logical. Print diagnostic messages.}

\item{parsed}{Internal. Pre-parsed source cache; if \code{NULL}, files are read
from \code{path} on demand.}
}
\value{
\code{\link[=checktor_check_result]{checktor_check_result()}} with \code{passed}, \code{issues}, \code{message}.
}
\description{
Flags bare \code{T} / \code{F} symbols that should be \code{TRUE} / \code{FALSE}. Operates on
the parsed syntax tree, so \code{T} inside a string or a comment is not reported,
which a plain text search could not tell apart. Named-argument names
(\code{f(T = 1)}) and \verb{$T} / \verb{@T} extractions are excluded.
}
\section{Source}{

No binding rule forbids \code{T} and \code{F}, though the CRAN Cookbook keeps a recipe for
it under
\href{https://contributor.r-project.org/cran-cookbook/code_issues.html#tf-instead-of-truefalse}{T/F Instead of TRUE/FALSE}.
They are ordinary variables (see \code{?logical}) that R sets to \code{TRUE} and \code{FALSE} at
startup but that any code can rebind, so a function reading \code{T} after something
has run \code{T <- 0} gets the wrong answer. A real risk that no rule makes citable is
why this sits at \code{robustness} tier rather than policy. See \code{vignette("check-sources", package = "checktor")} for how every
check maps to its source.
}

\examples{
# show_content defaults to TRUE, so the offending file prints first
pkg <- example_diagnose_scenario("code_examples/tf_usage_bad.R")
issues(lab_tf_usage(pkg, verbose = FALSE))
}
