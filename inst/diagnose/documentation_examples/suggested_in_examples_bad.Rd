\name{uses_suggest}
\alias{uses_suggest}
\title{Uses a Suggested Package}
\description{
A function whose example loads a Suggested package without a guard.
}
\usage{
uses_suggest()
}
\value{
A processed value.
}
\examples{
library(somesuggest)
somesuggest::do_thing()
}
